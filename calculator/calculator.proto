syntax = "proto3";

option go_package = "github.com/DeepAung/calculator-grpc/calculator";

package calculator;

// The greeting service definition.
service Calculator {
  rpc Calculate(Expression) returns (Result) {}
  // rpc Calculates(stream string expression) returns ()
}

message Expression { string expression = 1; }
message Result { double result = 1; }
